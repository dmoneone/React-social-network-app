const initialState = {
    sidebar : {
        friends : [{name: 'Alex'},{name: 'Kate'},{name: 'Jora'}]
    }
}

const navComponentReducer = (state = initialState,action) => {
    return state;
}

export default navComponentReducer;